{
  "name": "bainovia-companion-backend",
  "version": "1.0.0",
  "description": "Bainovia Companion API - Fastify backend with session auth and SQLite",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.1",
    "@fastify/cors": "^11.0.1",
    "@fastify/helmet": "^13.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.1.0",
    "fastify": "^5.3.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
